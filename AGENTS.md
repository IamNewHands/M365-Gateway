# 维护约束

- 以当前源码为唯一修改入口，部署前核对目标账号及原存储绑定。
- 不添加未经验证的模型，不擅自更换上游协议，不修改调用方工具参数来猜测用户意图。
- 修改配套回归测试；完整 npm run check 返回 0 才部署。
- 真实客户端串行验收；不修改本地客户端程序来掩盖网关故障。
- 不输出或提交任何真实 Secret、API Key、OAuth token、生产日志或会话。
- 记录实际部署版本、产物摘要及实测范围，不虚报完成或无限稳定。
